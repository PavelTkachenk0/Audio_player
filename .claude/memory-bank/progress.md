# Progress

## Сделано
- **Безопасность**: BCrypt-хеш паролей, секреты убраны из репо, двухшаговый 2FA, CORS-allowlist, защита `AudioHub`, отзыв и ротация токенов.
- **Функц. баги**: исправлены privacy избранного (scope по userId), Put-предикаты (`==`), рекомендации (Take/OrderBy/TotalCount), GetById (filter-before-Select), `Album.Id`.
- **Архитектура**: полный рефактор в сервисы + доменные мапперы по всем 9 областям + `AuthService`. Эндпоинты тонкие.
- **Инфра**: xUnit-тесты (23 кейса), Docker (compose + Postgres), резюме-grade README. Удалены: уязвимый AutoMapper, надуманные Quartz-джобы.

## В работе / план
- **Миграция на микросервисы — Этап 3**: разрез `Catalog ↔ Library` через брокер сообщений (RabbitMQ/MassTransit), read-модель + события (`TrackUpdated`), eventual consistency. Главный интерес — увидеть работу брокера.
- Полный план миграции (bounded contexts, карта данных, strangler-этапы) — обсуждался; Этап 3 выбран как первый к реализации.

## НЕ делать
- Не вводить репозитории поверх EF. Не возвращать inline-проекции (только мапперы). Не возвращать Quartz. Не коммитить секреты.
